---
phase: 02-transcript
plan: 04
type: execute
wave: 4
depends_on: ["02-03"]
files_modified:
  - tests/test_transcript/test_pipeline_integration.py
  - tests/test_transcript/test_dashboard_render.py
  - docs/setup_whisper.md
  - config/config.transcript.example.json
  - README.md
  - AGENTS.md
autonomous: true
requirements: [TRANSCRIPT-06]
user_setup: []
must_haves:
  truths:
    - "Integration tests cover every branch of the degradation matrix (no whisper, no model, no audio, whisper crash)"
    - "Dashboard rendering test asserts an injected <script> is rendered escaped, not executed"
    - "docs/setup_whisper.md provides copy-paste-ready install commands"
    - "config/config.transcript.example.json is a working example config with transcription enabled"
    - "README + AGENTS.md describe the new config keys and behavior"
  artifacts:
    - "tests/test_transcript/test_pipeline_integration.py"
    - "tests/test_transcript/test_dashboard_render.py"
    - "docs/setup_whisper.md"
    - "config/config.transcript.example.json"
  key_links:
    - "Integration tests use pytest fixtures with mocked whisper + mocked audio extraction"
    - "config.transcript.example.json references docs/setup_whisper.md in a __comment key"
---

<objective>
Ship Phase 2 with confidence: integration tests proving every failure
mode is handled, an XSS-safety test on the dashboard render, a setup
guide for whisper.cpp, and docs updated so users can turn on
transcription in one config edit.

Purpose: Turns Phase 2 from "works on my machine" into "reproducible,
tested, documented, shippable".
Output: Tests, docs, example config.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-transcript/02-03-SUMMARY.md
@src/transcript/pipeline_glue.py
@src/pipeline/pipeline_runner.py
@post_ocr_pipeline.py
@README.md
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Integration tests for the degradation matrix</name>
  <files>tests/test_transcript/test_pipeline_integration.py</files>
  <read_first>src/transcript/pipeline_glue.py, src/pipeline/pipeline_runner.py</read_first>
  <action>
Create `tests/test_transcript/test_pipeline_integration.py`:

Use pytest + unittest.mock. Each test patches `extract_audio` and/or `transcribe` to simulate a specific failure, then calls `kickoff_transcription()` and verifies:
- The pipeline function returns cleanly (no exception)
- The Future resolves to None (graceful skip)
- A log line captured via `caplog` contains the expected phrase

Test cases:

- `test_transcription_disabled_returns_none` — cfg={"enabled": False} → future is None, log says "disabled"
- `test_no_audio_stream_skips_gracefully` — extract_audio raises NoAudioStreamError → future resolves None, log contains "No audio"
- `test_missing_whisper_binary_skips_gracefully` — transcribe raises WhisperNotAvailableError → future resolves None, log contains "brew install whisper-cpp"
- `test_missing_model_skips_gracefully` — transcribe raises WhisperNotAvailableError with "download-ggml-model" → skip + log
- `test_whisper_crash_skips_gracefully` — transcribe raises WhisperFailureError → skip + log contains "whisper failed"
- `test_successful_transcription_persists_json` — mock transcribe returns 2 Segments, verify `metadata_dir/transcript.json` is written with normalized shape

Bonus test:
- `test_enrich_only_matched_frames` — pass 3 matched + 2 unmatched OCR results plus a mocked segment list; verify only the 3 matched dicts got transcript_context and the 2 unmatched dicts are the SAME OBJECT (identity check).
  </action>
  <verify>`pytest tests/test_transcript/test_pipeline_integration.py -v` — all pass.</verify>
  <acceptance_criteria>
    - "pytest tests/test_transcript/test_pipeline_integration.py reports >= 7 passed"
  </acceptance_criteria>
  <done>Every branch of the degradation matrix has a test.</done>
</task>

<task type="auto">
  <name>Task 2: Dashboard XSS + rendering test</name>
  <files>tests/test_transcript/test_dashboard_render.py</files>
  <read_first>post_ocr_pipeline.py</read_first>
  <action>
Create `tests/test_transcript/test_dashboard_render.py`:

- `test_html_escape_neutralizes_script_tags` — construct a fake pick with `stockPick = "<script>alert(1)</script>"` and `transcript_context.at = "<img src=x onerror=y>"`. Run the viewer.html generation on this fake data. Assert the resulting HTML contains `&lt;script&gt;` and `&lt;img` (escaped) and does NOT contain literal `<script>alert(1)</script>` or `<img src=x onerror`.
- `test_transcript_section_renders_when_context_present` — pick with non-empty transcript_context → HTML contains "Spoken context" and the actual quoted text (escaped).
- `test_transcript_section_absent_when_context_empty` — pick with no transcript_context → HTML does NOT contain "Spoken context".
- `test_transcript_section_absent_when_context_all_empty_strings` — pick with transcript_context but all "" values → also no rendered section.

To keep this testable, refactor the viewer.html generator into a callable function `build_dashboard_html(picks: list[dict]) -> str` if it's currently inline. Small refactor, big testability win.
  </action>
  <verify>`pytest tests/test_transcript/test_dashboard_render.py -v` — all pass.</verify>
  <acceptance_criteria>
    - "pytest tests/test_transcript/test_dashboard_render.py reports >= 4 passed"
    - "post_ocr_pipeline.py exposes a testable `build_dashboard_html(picks)` or equivalent function"
  </acceptance_criteria>
  <done>Dashboard render is refactored for testability and XSS-safety is regression-tested.</done>
</task>

<task type="auto">
  <name>Task 3: docs/setup_whisper.md</name>
  <files>docs/setup_whisper.md</files>
  <action>
Create `docs/setup_whisper.md` with these sections:

    # Setting Up whisper.cpp for LOCALOCR

    LOCALOCR uses whisper.cpp for local audio transcription (Phase 2
    feature). This guide walks through installation and verification.

    ## macOS (Apple Silicon)

    ```bash
    brew install whisper-cpp
    ```

    ## Download a Model

    The `base.en` model is recommended for English content
    (~150 MB, ~5-10x realtime on M-series Macs):

    ```bash
    mkdir -p ~/.whisper.cpp/models
    cd ~/.whisper.cpp/models
    curl -L -O https://huggingface.co/ggerganov/whisper.cpp/resolve/main/ggml-base.en.bin
    ```

    ## Verify

    ```bash
    whisper-cli --help | head -5
    ls -lh ~/.whisper.cpp/models/ggml-base.en.bin
    ```

    ## Enable in Config

    Add to your `config/config.json`:

    ```json
    {
      "transcript_config": {
        "enabled": true,
        "model": "base.en",
        "model_dir": "~/.whisper.cpp/models",
        "binary": "whisper-cli",
        "context_window_seconds": 8,
        "language": "en"
      }
    }
    ```

    ## Troubleshooting

    | Symptom | Fix |
    |---------|-----|
    | "whisper.cpp binary not found" | `brew install whisper-cpp` |
    | "whisper model not found" | Run the curl command above |
    | Transcription is slow | Try the smaller `tiny.en` model |
    | Non-English content | Change `language` and use a multilingual model (base, small, medium) |
  </action>
  <verify>`test -f docs/setup_whisper.md && wc -l docs/setup_whisper.md`.</verify>
  <acceptance_criteria>
    - "docs/setup_whisper.md exists"
    - "docs/setup_whisper.md contains 'brew install whisper-cpp'"
    - "docs/setup_whisper.md contains 'ggml-base.en.bin'"
  </acceptance_criteria>
  <done>Setup guide committed.</done>
</task>

<task type="auto">
  <name>Task 4: Example config</name>
  <files>config/config.transcript.example.json</files>
  <read_first>config/config.json</read_first>
  <action>
Copy `config/config.json` to `config/config.transcript.example.json` and add:

    {
      "__comment": "Example config with transcription enabled. See docs/setup_whisper.md.",
      ...
      "transcript_config": {
        "enabled": true,
        "model": "base.en",
        "model_dir": "~/.whisper.cpp/models",
        "binary": "whisper-cli",
        "context_window_seconds": 8,
        "language": "en"
      }
    }

Keep everything else identical to the reference config.
  </action>
  <verify>`python -c "import json; json.load(open('config/config.transcript.example.json'))"`.</verify>
  <acceptance_criteria>
    - "config/config.transcript.example.json is valid JSON containing 'transcript_config'"
  </acceptance_criteria>
  <done>Example config in place.</done>
</task>

<task type="auto">
  <name>Task 5: README + AGENTS.md updates</name>
  <files>README.md, AGENTS.md</files>
  <read_first>README.md, AGENTS.md</read_first>
  <action>
**README.md** — add a section "Speaker Attribution (Transcription)" that:
- Explains what it does in one paragraph
- Links to docs/setup_whisper.md
- Shows the 5-line config snippet
- Notes the graceful-degradation behavior (no whisper = feature skipped, everything else still works)

**AGENTS.md** — update:
- Configuration Schema table: add rows for `transcript_config.enabled`, `.model`, `.model_dir`, `.binary`, `.context_window_seconds`, `.language`
- Module tree: add `src/transcript/` package with its 4 modules (`audio_extractor.py`, `whisper_transcriber.py`, `correlator.py`, `pipeline_glue.py`)
- Architecture Summary diagram: add the transcription branch running parallel to OCR
- Post-OCR Pipeline section: note that vision prompts include transcript_context when present
- Metadata outputs table: add `output/metadata/transcript.json` row
- Dependencies: add whisper.cpp binary + ggml model
  </action>
  <verify>`grep -n 'transcript_config\|whisper.cpp' README.md AGENTS.md` returns matches in both files.</verify>
  <acceptance_criteria>
    - "README.md contains 'transcript_config' and 'whisper.cpp'"
    - "AGENTS.md contains 'src/transcript/' and 'transcript.json'"
  </acceptance_criteria>
  <done>Docs match reality.</done>
</task>

<task type="auto">
  <name>Task 6: Commit Phase 2</name>
  <files>—</files>
  <action>
Stage:

    git add src/common/ src/transcript/ src/pipeline/pipeline_runner.py \
            src/extractor/frame_extractor.py \
            src/analyzer/ollama_analyzer.py post_ocr_pipeline.py \
            tests/ config/config.transcript.example.json \
            docs/setup_whisper.md \
            README.md AGENTS.md \
            .planning/

Commit:

    Phase 2: whisper.cpp transcript attribution

    - Extract src/common/subprocess_utils.py (DRY refactor across
      frame_extractor + new audio_extractor)
    - Add src/transcript/ package: audio_extractor, whisper_transcriber,
      correlator, pipeline_glue
    - Pipeline runner kicks off transcription in a background thread
      parallel with OCR (wall-time overhead <= 30%)
    - Every matched frame gains a transcript_context field
      (before/at/after/speaker)
    - Vision prompts (post_ocr_pipeline) include transcript context
    - viewer.html renders collapsible Spoken Context per pick card
    - Fixed reviewer findings: B2 (step counter) and W6 (XSS)
    - 25+ new unit + integration tests
    - docs/setup_whisper.md and config/config.transcript.example.json

    Requirements: TRANSCRIPT-01..TRANSCRIPT-09
  </action>
  <verify>`git status` clean; `git log -1` shows commit.</verify>
  <acceptance_criteria>
    - "`git status --porcelain` is empty"
    - "`git log -1 --oneline` mentions 'Phase 2' or 'transcript'"
  </acceptance_criteria>
  <done>Phase 2 shipped.</done>
</task>

</tasks>

<verification>
- [ ] `pytest tests/ -v` — every test passes (Phase 1 tests + all Phase 2 tests)
- [ ] End-to-end run with transcription enabled produces a viewer.html with visible "Spoken context" sections
- [ ] End-to-end run with `transcript_config.enabled: false` still works
- [ ] `docs/setup_whisper.md` renders correctly
- [ ] `git status` is clean
</verification>

<success_criteria>

- Full degradation matrix regression-tested
- XSS regression-tested (finding W6 stays fixed forever)
- User can go from "curious" to "running transcription" in 3 shell commands + 1 config edit
- Phase 2 committed as one clean unit

</success_criteria>

<output>
After completion, create `.planning/phases/02-transcript/02-04-SUMMARY.md` and update `.planning/STATE.md` to mark Phase 2 complete.
</output>

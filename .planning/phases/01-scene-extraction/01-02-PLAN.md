---
phase: 01-scene-extraction
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/extractor/frame_extractor.py
  - tests/test_frame_extractor.py
autonomous: true
requirements: [EXTRACT-01, EXTRACT-02, EXTRACT-04, EXTRACT-05]
user_setup: []
must_haves:
  truths:
    - "extraction_mode: 'scene' triggers ffmpeg scene-change detection via `select='gt(scene,THRESHOLD)'` filter"
    - "extraction_mode: 'hybrid' runs TWO ffmpeg passes (scene + fixed-interval) and merges the timestamp lists — does NOT use the broken `eq(mod(t,N),0)` filter"
    - "PTS timestamps parsed from ffmpeg `showinfo` filter drive the frame filenames (real timestamps, not synthetic)"
    - "Debounce operates on `(file, pts)` pairs and deletes dropped files so D5 (real PTS) is never silently violated"
    - "Invalid scene_config values (threshold out of [0,1], negative gaps) fail fast"
  artifacts:
    - "src/extractor/frame_extractor.py (scene + hybrid extractors added; _debounce_pairs helper added)"
    - "tests/test_frame_extractor.py (fixture-based PTS parsing + debounce pair tests + hybrid two-pass semantic test + negative assertion against eq(mod filter)"
    - "tests/fixtures/showinfo_stderr.txt (canned ffmpeg stderr sample)"
  key_links:
    - "_EXTRACTORS['scene'] → _extract_by_scene → _parse_showinfo_pts → _debounce_pairs (deletes dropped files) → _finalize_frames"
    - "_EXTRACTORS['hybrid'] → _extract_by_hybrid (two-pass: scene pass + fps=1/max_gap pass) → merge via _debounce_pairs → _finalize_frames"
---

<objective>
Add scene-change and hybrid extraction modes to
`src/extractor/frame_extractor.py`. Reuse the strategy-dispatch shape
from plan 01-01. Parse actual PTS values from ffmpeg's `showinfo`
filter so frame filenames carry real timestamps (not synthetic
`(N-1)*interval` math). Debounce close-together scene fires.

Purpose: The core value delivery for Phase 1 — ≥5x frame reduction on
broadcast content, powering all downstream efficiency gains.
Output: `scene` and `hybrid` extraction modes fully working with tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-scene-extraction/01-01-SUMMARY.md
@src/extractor/frame_extractor.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactor `_run_ffmpeg` to optionally return stderr</name>
  <files>src/extractor/frame_extractor.py</files>
  <read_first>src/extractor/frame_extractor.py</read_first>
  <action>
Current `_run_ffmpeg(cmd, video_label, timeout)` returns `None` and swallows stderr on success. Add a `capture_stderr: bool = False` parameter. When true, return `result.stderr` as a string (empty string on empty stderr) instead of `None`. When false, preserve current behavior exactly (returns None, existing callers unaffected).

Do NOT change signature ordering — keep `capture_stderr` as a keyword-only argument with a default:

    def _run_ffmpeg(cmd, video_label, timeout, *, capture_stderr: bool = False):
  </action>
  <verify>Existing interval extraction still works (`python main.py ./config/config.json` unchanged output).</verify>
  <acceptance_criteria>
    - "src/extractor/frame_extractor.py contains '*, capture_stderr: bool = False'"
  </acceptance_criteria>
  <done>Function signature updated, existing callers unaffected.</done>
</task>

<task type="auto">
  <name>Task 2: Add `_parse_showinfo_pts` helper</name>
  <files>src/extractor/frame_extractor.py, tests/fixtures/showinfo_stderr.txt</files>
  <action>
Add module-level regex + helper:

    _PTS_RE = re.compile(r"pts_time:(\d+\.?\d*)")

    def _parse_showinfo_pts(stderr: str) -> list[float]:
        """Extract PTS timestamps (seconds) from ffmpeg showinfo stderr."""
        return sorted(float(m.group(1)) for m in _PTS_RE.finditer(stderr))

Create `tests/fixtures/showinfo_stderr.txt` with a realistic canned sample (5-10 showinfo lines with varying pts_time values, plus some non-matching noise lines).

Sample fixture content (real-world shape):

    [Parsed_showinfo_1 @ 0x1234] n:   0 pts:   180 pts_time:6.000000 duration_time:0.040000 …
    [Parsed_showinfo_1 @ 0x1234] n:   1 pts:   540 pts_time:18.000000 duration_time:0.040000 …
    [Parsed_showinfo_1 @ 0x1234] n:   2 pts:   890 pts_time:29.666667 duration_time:0.040000 …
    [some unrelated warning line]
    [Parsed_showinfo_1 @ 0x1234] n:   3 pts:  1200 pts_time:40.000000 duration_time:0.040000 …
  </action>
  <verify>Unit test in Task 6 exercises this.</verify>
  <acceptance_criteria>
    - "src/extractor/frame_extractor.py contains 'def _parse_showinfo_pts('"
    - "tests/fixtures/showinfo_stderr.txt exists and is non-empty"
  </acceptance_criteria>
  <done>Helper + fixture in place.</done>
</task>

<task type="auto">
  <name>Task 3: Add _debounce_timestamps AND _debounce_pairs helpers</name>
  <files>src/extractor/frame_extractor.py</files>
  <action>
Add TWO pure-function helpers. `_debounce_timestamps` stays for the standalone-timestamp use case (and is tested for shape); `_debounce_pairs` is the one production extractors will call, so file cleanup can happen at the caller.

    def _debounce_timestamps(ts: list[float], min_gap: float) -> list[float]:
        """Drop timestamps within `min_gap` seconds of the previously kept one."""
        if min_gap <= 0 or not ts:
            return list(ts)
        kept = [ts[0]]
        for t in ts[1:]:
            if t - kept[-1] >= min_gap:
                kept.append(t)
        return kept

    def _debounce_pairs(
        pairs: list[tuple[Path, float]],
        min_gap: float,
    ) -> list[tuple[Path, float]]:
        """Debounce (file, pts) pairs by PTS gap. Returns survivors in PTS order.
        Caller is responsible for unlinking any files NOT in the returned list."""
        if min_gap <= 0 or not pairs:
            return list(pairs)
        ordered = sorted(pairs, key=lambda fp: fp[1])
        kept = [ordered[0]]
        for f, t in ordered[1:]:
            if t - kept[-1][1] >= min_gap:
                kept.append((f, t))
        return kept

Both are pure. Neither touches the filesystem. The tmp-file unlink happens in the SCENE and HYBRID extractors (Task 4).
  </action>
  <verify>Unit tests in Task 6.</verify>
  <acceptance_criteria>
    - "src/extractor/frame_extractor.py contains 'def _debounce_timestamps('"
    - "src/extractor/frame_extractor.py contains 'def _debounce_pairs('"
  </acceptance_criteria>
  <done>Both helpers defined; neither has side effects.</done>
</task>

<task type="auto">
  <name>Task 4: Add _extract_by_scene (pair-debounce + file unlink) and _extract_by_hybrid (two-pass merge)</name>
  <files>src/extractor/frame_extractor.py</files>
  <action>
**Fixes plan-check BLOCKERs 2 (broken hybrid filter) and 3 (silent D5 violation).** These are the shapes locked in the plan frontmatter's `key_links` — Task 3's helpers are used verbatim here.

---

**_extract_by_scene** — debounces `(file, pts)` pairs and deletes dropped tmp files so `_finalize_frames` sees a directory whose contents match `kept_ts` 1:1. The synthetic-timestamp fallback branch is preserved ONLY for the genuine ffmpeg/parser drift case (kept as D5-compliant safety net with a WARNING log).

    def _extract_by_scene(video, out_path, tmp_dir, ffmpeg_bin, duration, cfg):
        scene_cfg = cfg.get("scene_config", {}) or {}
        threshold = float(scene_cfg.get("threshold", 0.3))
        min_gap = float(scene_cfg.get("min_gap_seconds", 1.0))

        tmp_pattern = str(tmp_dir / "frame_%04d.png")
        cmd = [
            ffmpeg_bin, "-hide_banner", "-loglevel", "info",
            "-i", str(video),
            "-vf", f"select='gt(scene,{threshold})',showinfo",
            "-vsync", "vfr",
            tmp_pattern,
        ]
        timeout = max(300, int(duration) * 3)
        stderr = _run_ffmpeg(cmd, str(video), timeout, capture_stderr=True)

        raw_ts = _parse_showinfo_pts(stderr)
        tmp_frames = sorted(tmp_dir.glob("frame_*.png"))

        # ffmpeg writes one file per selected frame; PTS list should be same length.
        # If NOT, we have real parser/container drift — fall back with a warning.
        if len(tmp_frames) != len(raw_ts):
            logger.warning(
                "Scene extraction: file count %d != PTS count %d BEFORE debounce "
                "— parser or container drift, using synthetic timestamps as fallback",
                len(tmp_frames), len(raw_ts),
            )
            interval = cfg.get("frame_interval_seconds", 2)
            synthetic = [i * interval for i in range(len(tmp_frames))]
            return _finalize_frames(tmp_dir, out_path, synthetic)

        # Debounce (file, pts) pairs together; delete files that lost the debounce
        # so _finalize_frames sees exactly the surviving frames.
        all_pairs = list(zip(tmp_frames, raw_ts))
        kept_pairs = _debounce_pairs(all_pairs, min_gap)
        kept_files = {f for f, _ in kept_pairs}
        for tmp_file, _ in all_pairs:
            if tmp_file not in kept_files:
                tmp_file.unlink(missing_ok=True)

        kept_ts = [p for _, p in kept_pairs]
        return _finalize_frames(tmp_dir, out_path, kept_ts)

---

**_extract_by_hybrid** — two-pass merge. The previous `eq(mod(t, max_gap), 0)` filter was semantically broken (floats almost never hit exact mod-zero boundaries). Instead, run TWO ffmpeg invocations — scene pass + fixed-interval pass — and merge their `(file, pts)` lists via `_debounce_pairs`.

    def _extract_by_hybrid(video, out_path, tmp_dir, ffmpeg_bin, duration, cfg):
        scene_cfg = cfg.get("scene_config", {}) or {}
        threshold = float(scene_cfg.get("threshold", 0.3))
        min_gap = float(scene_cfg.get("min_gap_seconds", 1.0))
        max_gap = float(scene_cfg.get("max_gap_seconds", 10.0))

        # Pass A: scene detection into a scoped subdir
        scene_dir = tmp_dir / "_scene"
        scene_dir.mkdir(exist_ok=True)
        scene_cmd = [
            ffmpeg_bin, "-hide_banner", "-loglevel", "info",
            "-i", str(video),
            "-vf", f"select='gt(scene,{threshold})',showinfo",
            "-vsync", "vfr",
            str(scene_dir / "frame_%04d.png"),
        ]
        scene_stderr = _run_ffmpeg(scene_cmd, str(video),
                                    max(300, int(duration) * 3),
                                    capture_stderr=True)
        scene_pts = _parse_showinfo_pts(scene_stderr)
        scene_files = sorted(scene_dir.glob("frame_*.png"))

        # Pass B: fixed-interval tick at max_gap into another scoped subdir
        gap_dir = tmp_dir / "_gap"
        gap_dir.mkdir(exist_ok=True)
        gap_cmd = [
            ffmpeg_bin, "-hide_banner", "-loglevel", "info",
            "-i", str(video),
            "-vf", f"fps=1/{max_gap},showinfo",
            "-vsync", "vfr",
            str(gap_dir / "frame_%04d.png"),
        ]
        gap_stderr = _run_ffmpeg(gap_cmd, str(video),
                                  max(300, int(duration) * 3),
                                  capture_stderr=True)
        gap_pts = _parse_showinfo_pts(gap_stderr)
        gap_files = sorted(gap_dir.glob("frame_*.png"))

        # Symmetric drift guard — mirrors _extract_by_scene's D5 fallback rigor.
        # If either pass has file/PTS drift, log a warning and trim to the
        # shorter list so `zip` doesn't silently discard anything.
        if len(scene_files) != len(scene_pts):
            logger.warning(
                "Hybrid scene pass drift: %d files vs %d PTS — trimming",
                len(scene_files), len(scene_pts),
            )
            n = min(len(scene_files), len(scene_pts))
            scene_files, scene_pts = scene_files[:n], scene_pts[:n]
        if len(gap_files) != len(gap_pts):
            logger.warning(
                "Hybrid gap pass drift: %d files vs %d PTS — trimming",
                len(gap_files), len(gap_pts),
            )
            n = min(len(gap_files), len(gap_pts))
            gap_files, gap_pts = gap_files[:n], gap_pts[:n]

        # Merge — debounce combined (file, pts) list. sort-stable in _debounce_pairs
        # keeps scene entries first when a scene frame and a gap frame have equal PTS.
        combined = list(zip(scene_files, scene_pts)) + list(zip(gap_files, gap_pts))
        kept = _debounce_pairs(combined, min_gap)
        kept_files_set = {f for f, _ in kept}

        # Move survivors into tmp_dir with fresh sequential names so the existing
        # _finalize_frames contract (frame_NNNN.png inputs) holds.
        final_ts = []
        for i, (src, pts) in enumerate(kept, start=1):
            dst = tmp_dir / f"frame_{i:04d}.png"
            src.rename(dst)
            final_ts.append(pts)

        # Delete losers and clean up scoped subdirs.
        for f in scene_files + gap_files:
            if f.exists() and f not in kept_files_set:
                f.unlink(missing_ok=True)
        for d in (scene_dir, gap_dir):
            try:
                d.rmdir()
            except OSError:
                pass

        return _finalize_frames(tmp_dir, out_path, final_ts)

---

**Register both in _EXTRACTORS:**

    _EXTRACTORS = {
        "interval": _extract_by_interval,
        "scene":    _extract_by_scene,
        "hybrid":   _extract_by_hybrid,
    }

Remove the "not yet implemented" placeholder from plan 01-01.

**Explicit non-goal: the string `eq(mod(t,` must appear in NO ffmpeg command anywhere in this module.** Task 6 has a negative assertion that will fail CI if it does.
  </action>
  <verify>
Run `python main.py` with `extraction_mode: "scene"` against a real video. Confirm the pipeline completes and produces fewer frames than the interval baseline. Spot-check 2-3 frame timestamps in filenames — they should match actual visual scene changes (real PTS, not synthetic).

Run with `extraction_mode: "hybrid"` against a video that includes a long static shot (>max_gap seconds unchanging). Confirm that section produces at least one frame (proves the fallback tick actually fires, unlike the old modulo filter).
  </verify>
  <acceptance_criteria>
    - "src/extractor/frame_extractor.py contains 'def _extract_by_scene('"
    - "src/extractor/frame_extractor.py contains 'def _extract_by_hybrid('"
    - "src/extractor/frame_extractor.py `_EXTRACTORS` dict has 3 entries"
    - "grep 'eq(mod(t,' src/extractor/frame_extractor.py returns ZERO matches (negative assertion for BLOCKER 2)"
    - "_extract_by_scene calls _debounce_pairs and unlinks files not in the kept set"
    - "_extract_by_hybrid runs _run_ffmpeg TWICE (scene pass + fps=1/max_gap pass) then merges via _debounce_pairs"
  </acceptance_criteria>
  <done>Scene and hybrid modes execute end-to-end with real PTS-driven timestamps; broken modulo filter is nowhere in the module.</done>
</task>

<task type="auto">
  <name>Task 5: Extend `_validate_extraction_config`</name>
  <files>src/extractor/frame_extractor.py</files>
  <action>
Extend the helper from plan 01-01 to validate `scene_config` when mode is `"scene"` or `"hybrid"`:

- `threshold` must be a float in `[0.0, 1.0]`
- `min_gap_seconds` must be `>= 0`
- `max_gap_seconds` must be `> 0` (hybrid only)

All failures raise `FrameExtractionError` with the offending key + value in the message.
  </action>
  <verify>Feed invalid configs (threshold=2.0, min_gap_seconds=-1) and confirm fail-fast.</verify>
  <acceptance_criteria>
    - "_validate_extraction_config rejects threshold > 1.0 with FrameExtractionError"
    - "_validate_extraction_config rejects negative gaps with FrameExtractionError"
  </acceptance_criteria>
  <done>Validation catches bad configs before ffmpeg is invoked.</done>
</task>

<task type="auto">
  <name>Task 6: Unit tests — shape + semantic + negative assertions</name>
  <files>tests/test_frame_extractor.py</files>
  <action>
Add these tests. **Note the two new categories: semantic tests that assert two ffmpeg passes for hybrid, and negative assertions that the broken modulo filter is nowhere used.**

- `test_parse_showinfo_pts_extracts_all_timestamps` — load `tests/fixtures/showinfo_stderr.txt`, call `_parse_showinfo_pts`, assert `[6.0, 18.0, 29.666667, 40.0]`.
- `test_parse_showinfo_pts_empty_stderr` — returns `[]`.
- `test_debounce_drops_close_frames` — `_debounce_timestamps([0.0, 0.5, 1.5, 1.7, 3.0], 1.0)` returns `[0.0, 1.5, 3.0]`.
- `test_debounce_min_gap_zero_is_noop` — returns input unchanged.
- `test_debounce_pairs_returns_survivor_pairs` — build `[(Path('a'),0.0),(Path('b'),0.5),(Path('c'),1.5),(Path('d'),3.0)]`, call `_debounce_pairs(..., min_gap=1.0)`, assert exactly `[(Path('a'),0.0),(Path('c'),1.5),(Path('d'),3.0)]`. Path('b') is the dropped survivor.
- `test_debounce_pairs_is_pure_no_filesystem_side_effects` — with real temp Path objects that DO exist on disk, run `_debounce_pairs`, then assert ALL original files still exist (pure function — caller does the unlink, not the helper).
- `test_scene_mode_ffmpeg_command_shape` — mock subprocess.run, call `_extract_by_scene`, assert the ffmpeg command list contains `"select='gt(scene,0.3)',showinfo"`.
- `test_scene_mode_deletes_debounced_tmp_files` — pre-populate tmp_dir with 3 fake frame files, mock `_parse_showinfo_pts` to return `[0.0, 0.5, 2.0]` and `_run_ffmpeg` to be a no-op. With `min_gap=1.0`, run `_extract_by_scene`. Assert only 2 frame files remain on disk (the debounced middle one is unlinked). This is the semantic proof for BLOCKER 3 fix.
- `test_hybrid_mode_runs_two_ffmpeg_passes` — mock `_run_ffmpeg` with a call-counting side_effect. Run `_extract_by_hybrid`. Assert `_run_ffmpeg` was invoked EXACTLY twice; assert the first call's cmd contains `"select='gt(scene,"`; assert the second call's cmd contains `"fps=1/"`. Semantic proof for BLOCKER 2 fix.
- `test_hybrid_mode_does_not_use_eq_mod_filter` — collect ALL ffmpeg cmd strings during a hybrid extraction; assert `"eq(mod(t,"` appears in ZERO of them. Negative regression fence.
- `test_frame_extractor_source_has_no_eq_mod_filter` — read `src/extractor/frame_extractor.py` as text, assert `"eq(mod(t,"` is not a substring. Guards against future editors accidentally reintroducing the broken filter.
- `test_invalid_scene_threshold_raises` — cfg with `scene_config: {threshold: 2.0}` raises FrameExtractionError.
- `test_invalid_scene_negative_gap_raises` — cfg with `min_gap_seconds: -1` raises.
  </action>
  <verify>`pytest tests/test_frame_extractor.py -v` — all pass.</verify>
  <acceptance_criteria>
    - "Running `pytest tests/test_frame_extractor.py` reports at least 15 passed"
    - "Test suite includes `test_hybrid_mode_runs_two_ffmpeg_passes` (BLOCKER 2 semantic proof)"
    - "Test suite includes `test_hybrid_mode_does_not_use_eq_mod_filter` (BLOCKER 2 negative fence)"
    - "Test suite includes `test_frame_extractor_source_has_no_eq_mod_filter` (source-level regression fence)"
    - "Test suite includes `test_scene_mode_deletes_debounced_tmp_files` (BLOCKER 3 semantic proof)"
  </acceptance_criteria>
  <done>All tests pass; both blockers are regression-fenced at semantic AND source-text levels.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pytest tests/test_frame_extractor.py -v` — all pass
- [ ] Manual: create `config/config.scene.example.json` with `extraction_mode: "scene"`, run pipeline, confirm frames extracted and pipeline finishes
- [ ] Manual: same for `hybrid`
- [ ] Frame count in scene mode is at least 3x smaller than interval mode on the same video (looser than the 5x target — the benchmark in plan 01-03 proves the tighter number)
</verification>

<success_criteria>

- All three extraction modes (interval, scene, hybrid) fully implemented
- PTS parsing, debounce, config validation all in place
- 11+ unit tests passing
- Manual smoke test on a real video shows meaningful frame reduction

</success_criteria>

<output>
After completion, create `.planning/phases/01-scene-extraction/01-02-SUMMARY.md`
</output>

---
phase: 01-scene-extraction
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/extractor/frame_extractor.py
  - src/pipeline/pipeline_runner.py
  - tests/test_frame_extractor.py
  - tests/fixtures/interval_baseline_manifest.json
autonomous: true
requirements: [EXTRACT-03, EXTRACT-05]
user_setup: []
must_haves:
  truths:
    - "Running `python main.py ./config/config.json` with any existing config produces byte-identical output to the pre-refactor build (proven by sha256 manifest test, not just file listing)"
    - "extract_frames() accepts an optional `cfg: dict | None = None` kwarg; when None/empty, behavior is identical to today"
    - "pipeline_runner.py contains exactly ONE new hunk: adding `cfg=config` to the extract_frames call"
    - "extract_frames() delegates to one of three strategy functions based on cfg['extraction_mode']"
    - "Frame naming convention frame_NNNN_XXmYYs.png is preserved by a shared _finalize_frames() helper that parses NNNN via regex (matches current warn-and-skip behavior for out-of-band files)"
  artifacts:
    - "src/extractor/frame_extractor.py (refactored — dispatch table + widened signature + _extract_by_interval helper + _finalize_frames helper)"
    - "src/pipeline/pipeline_runner.py (one-line change: passes cfg=config to extract_frames)"
    - "tests/test_frame_extractor.py (interval-mode backward compat + regex-numbering + validation tests)"
    - "tests/fixtures/interval_baseline_manifest.json (sha256 manifest of pre-refactor frames — byte-level regression fence)"
  key_links:
    - "extract_frames(cfg=config) → _EXTRACTORS[mode] → _extract_by_interval() → _finalize_frames()"
    - "pipeline_runner.py passes the full config dict through so downstream OCR-time helpers can also read from cfg without further plumbing"
    - "Byte-identical guarantee is enforced by tests/fixtures/interval_baseline_manifest.json + test_interval_mode_byte_identical_to_baseline"
---

<objective>
Refactor `src/extractor/frame_extractor.py` into a strategy-pattern
dispatch WITHOUT changing any observable behavior. Extract the current
inline interval logic into a helper `_extract_by_interval`. Extract the
tmp-file → final-name rename dance into `_finalize_frames`. Add a
dispatch dict keyed on `extraction_mode` config (only `"interval"`
implemented in this plan — scene/hybrid land in 01-02).

Purpose: Zero-risk foundational refactor. Every downstream plan builds
on this shape. Doing the refactor cleanly here means 01-02 is a pure
addition (Open/Closed principle: open for extension, closed for
modification).
Output: Refactored `frame_extractor.py` + first unit test proving
backward compat.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/extractor/frame_extractor.py
@src/pipeline/pipeline_runner.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract _finalize_frames helper (regex-based numbering preserved)</name>
  <files>src/extractor/frame_extractor.py</files>
  <read_first>src/extractor/frame_extractor.py</read_first>
  <action>
Extract the block that iterates `tmp_frames`, computes timestamps, renames files, and builds the result dicts into a new module-level helper:

    def _finalize_frames(
        tmp_dir: Path,
        out_path: Path,
        timestamps: list[float],
    ) -> list[dict]:

**Frame-number derivation — preserve current behavior exactly.** The existing code parses `NNNN` from each `frame_NNNN.png` tmp filename via `_seq_re` and logs a warning if the regex misses (then skips the file). Reproduce this exactly:

    _seq_re = re.compile(r"frame_(\d+)\.png$")
    for i, tmp_file in enumerate(sorted(tmp_dir.glob("frame_*.png"))):
        m = _seq_re.search(tmp_file.name)
        if not m:
            logger.warning("Ignoring unexpected file: %s", tmp_file.name)
            continue
        frame_number = int(m.group(1))
        ts_seconds = int(round(timestamps[i]))
        frame_name = f"frame_{frame_number:04d}_{_format_timestamp(ts_seconds)}.png"
        ...

Do NOT switch to `i + 1` for frame_number — that would misnumber all frames if an out-of-band file exists (regression identified by plan-check WARNING 7). Timestamps[i] is looked up by position in the sorted tmp-file list, matching how the caller must build the list.

The helper returns the list of frame dicts (`frame_path`, `frame_name`, `timestamp`, `frame_number`). Pure function — no config access, only debug-level logging.
  </action>
  <verify>
Run the existing pipeline against a real video and compute `sha256sum output/all_frames/*.png` before vs after this task. Checksums must match. (Formal manifest test lands in Task 5.)
  </verify>
  <acceptance_criteria>
    - "src/extractor/frame_extractor.py contains 'def _finalize_frames('"
    - "_finalize_frames still parses NNNN via _seq_re (not `i + 1`)"
    - "extract_frames() no longer contains the inline rename loop"
  </acceptance_criteria>
  <done>Helper exists, is called by the current code path, and produces byte-identical output.</done>
</task>

<task type="auto">
  <name>Task 2: Widen extract_frames() signature + update pipeline_runner (per revised D8)</name>
  <files>src/extractor/frame_extractor.py, src/pipeline/pipeline_runner.py</files>
  <read_first>src/extractor/frame_extractor.py, src/pipeline/pipeline_runner.py</read_first>
  <action>
**Fixes plan-check BLOCKER 1.** `extract_frames()` needs a way to see `extraction_mode` / `scene_config`. CONTEXT.md D8 (revised) allows exactly one mechanical passthrough edit.

Step 2a — widen the signature:

    def extract_frames(
        video_path: str,
        output_dir: str,
        interval_seconds: int = 2,
        cfg: dict | None = None,   # NEW — keyword-only in practice
    ) -> list:
        cfg = cfg or {}
        ...

When `cfg` is empty/None, behavior is identical to today. This is D2's guarantee.

Step 2b — update the sole caller `src/pipeline/pipeline_runner.py` (search for `extract_frames(video_path, str(frames_dir), interval)`):

    frames = extract_frames(video_path, str(frames_dir), interval, cfg=config)

Nothing else in `pipeline_runner.py` changes. No new logic. No new logs. Zero behavioral impact when the config lacks `extraction_mode`.

Step 2c — the OCR-only entry (`run_ocr_only_pipeline`) does NOT call `extract_frames` so it needs no change.
  </action>
  <verify>
Run `python main.py ./config/config.json` against a real video with no `extraction_mode` in config. Frame count and filenames identical to pre-refactor. Add a temporary print in `extract_frames` confirming `cfg` is now populated.
  </verify>
  <acceptance_criteria>
    - "src/extractor/frame_extractor.py signature line contains 'cfg: dict | None = None'"
    - "src/pipeline/pipeline_runner.py contains 'cfg=config' in its extract_frames call"
    - "pipeline_runner.py has no other diff hunks (grep for any other new lines — must be zero)"
  </acceptance_criteria>
  <done>Signature widened, one-line passthrough added, backward-compat preserved.</done>
</task>

<task type="auto">
  <name>Task 3: Extract _extract_by_interval helper</name>
  <files>src/extractor/frame_extractor.py</files>
  <read_first>src/extractor/frame_extractor.py</read_first>
  <action>
Move the ffmpeg-command-build + `_run_ffmpeg` invocation for fixed-fps extraction into a new helper:

    def _extract_by_interval(
        video: Path,
        out_path: Path,
        tmp_dir: Path,
        ffmpeg_bin: str,
        duration: float,
        cfg: dict,
    ) -> list[dict]:
        interval = cfg.get("frame_interval_seconds", 2)
        # ... builds `-vf fps=1/interval` cmd, runs ffmpeg ...
        tmp_frames = sorted(tmp_dir.glob("frame_*.png"))
        # Interval mode: timestamps[i] = i * interval  (matches (frame_number - 1) * interval
        # for regex-parsed frame_number = i + 1)
        timestamps = [i * interval for i in range(len(tmp_frames))]
        return _finalize_frames(tmp_dir, out_path, timestamps)

`extract_frames()` becomes a thin dispatcher:

1. Validates inputs (existing `_validate_inputs`)
2. Resolves ffmpeg / ffprobe / duration / out_path / tmp_dir (existing setup)
3. Validates `cfg.get("extraction_mode", "interval")` (via Task 4)
4. Dispatches via a module-level dict:

    _EXTRACTORS = {
        "interval": _extract_by_interval,
        # "scene":   TODO — plan 01-02
        # "hybrid":  TODO — plan 01-02
    }

For now, only `"interval"` mode dispatches. Any other value raises `FrameExtractionError` with message: `"extraction_mode {v!r} not yet implemented (lands in plan 01-02)"`.

Default mode when config omits `extraction_mode` is `"interval"`.
  </action>
  <verify>Run `python main.py ./config/config.json` with the current config; confirm identical output to Task 1's baseline.</verify>
  <acceptance_criteria>
    - "src/extractor/frame_extractor.py contains 'def _extract_by_interval('"
    - "src/extractor/frame_extractor.py contains '_EXTRACTORS = {'"
    - "src/extractor/frame_extractor.py `_EXTRACTORS['interval']` maps to `_extract_by_interval`"
  </acceptance_criteria>
  <done>Dispatch table exists, interval mode routes through it, config without `extraction_mode` defaults to interval.</done>
</task>

<task type="auto">
  <name>Task 4: Add _validate_extraction_config helper</name>
  <files>src/extractor/frame_extractor.py</files>
  <read_first>src/extractor/frame_extractor.py</read_first>
  <action>
Add a `_validate_extraction_config(cfg: dict) -> str` helper called at the start of `extract_frames()` (after `cfg = cfg or {}` from Task 2). It:

- Reads `cfg.get("extraction_mode", "interval")`, lowercases it, and returns it.
- If value not in `_EXTRACTORS.keys()`, raise `FrameExtractionError(f"extraction_mode {v!r} invalid. Must be one of: {sorted(_EXTRACTORS)}")`.

Scene/hybrid-specific validation (threshold, gaps) is deferred to plan 01-02.
  </action>
  <verify>Feed an invalid `extraction_mode: "bogus"` and confirm fail-fast.</verify>
  <acceptance_criteria>
    - "src/extractor/frame_extractor.py contains 'def _validate_extraction_config('"
    - "extract_frames() calls _validate_extraction_config before any ffmpeg work"
  </acceptance_criteria>
  <done>Invalid mode fails fast with clear message.</done>
</task>

<task type="auto">
  <name>Task 5: Byte-level backward-compat manifest test (fixes plan-check WARNING 1)</name>
  <files>tests/fixtures/interval_baseline_manifest.json, tests/test_frame_extractor.py</files>
  <action>
Before ANY code from Task 1-3 is committed, record a manifest of the current build's output against a small test video.

Step 5a — record baseline (do this ONCE, from the pre-refactor branch head or a stash):

    # Pick smallest video in input_videos/ (or generate one via ffmpeg lavfi if none)
    python main.py ./config/config.json
    python -c "
    import hashlib, json, os
    from pathlib import Path
    manifest = {}
    for p in sorted(Path('output/all_frames').glob('*.png')):
        h = hashlib.sha256(p.read_bytes()).hexdigest()
        manifest[p.name] = {'size': p.stat().st_size, 'sha256': h}
    Path('tests/fixtures').mkdir(exist_ok=True, parents=True)
    Path('tests/fixtures/interval_baseline_manifest.json').write_text(
        json.dumps({'video': 'input_videos/<name>', 'frames': manifest}, indent=2))
    print(f'Recorded {len(manifest)} frames')
    "

Step 5b — write a regression test `test_interval_mode_byte_identical_to_baseline`:

    def test_interval_mode_byte_identical_to_baseline(tmp_path):
        """D2: byte-identical output for v1.0-shaped configs."""
        import hashlib, json, shutil
        from pathlib import Path
        from src.extractor import extract_frames

        manifest = json.loads(
            Path('tests/fixtures/interval_baseline_manifest.json').read_text()
        )
        video = manifest['video']
        if not Path(video).exists():
            pytest.skip(f'Baseline video {video} not in repo')

        out = tmp_path / 'frames'
        extract_frames(video, str(out), interval_seconds=2)  # no cfg — v1.0 shape

        actual = {}
        for p in sorted(out.glob('*.png')):
            actual[p.name] = {
                'size': p.stat().st_size,
                'sha256': hashlib.sha256(p.read_bytes()).hexdigest(),
            }
        assert actual == manifest['frames'], (
            'Byte-level regression vs baseline. Diffs:\n' +
            _diff(actual, manifest['frames'])
        )

Mark the test with `@pytest.mark.slow` since it runs real ffmpeg. Not skipped by default; skipped only when the baseline video is missing (dev machines / CI without the video).
  </action>
  <verify>`pytest tests/test_frame_extractor.py::test_interval_mode_byte_identical_to_baseline -v` — passes (or cleanly skips with a helpful message if baseline video absent).</verify>
  <acceptance_criteria>
    - "tests/fixtures/interval_baseline_manifest.json exists and contains at least 1 frame entry with sha256"
    - "tests/test_frame_extractor.py contains 'test_interval_mode_byte_identical_to_baseline'"
  </acceptance_criteria>
  <done>D2 is regression-tested at the byte level, not just file-listing level.</done>
</task>

<task type="auto">
  <name>Task 6: Unit tests — fully specified mock stack (fixes plan-check WARNING 2)</name>
  <files>tests/test_frame_extractor.py, tests/__init__.py</files>
  <action>
Create `tests/` directory and `tests/test_frame_extractor.py`.

- `test_finalize_frames_names_files_correctly` — create tmp files `frame_0001.png`, `frame_0002.png`, `frame_0003.png` (empty), call `_finalize_frames(tmp_dir, out_path, [0.0, 2.0, 4.0])`, assert `frame_0001_00m00s.png`, `frame_0002_00m02s.png`, `frame_0003_00m04s.png` exist and returned dicts are correctly shaped.

- `test_finalize_frames_preserves_frame_number_via_regex` — create tmp files `frame_0005.png`, `frame_0007.png` (skip 6 to prove regex-based numbering, not `i+1`). Assert output names are `frame_0005_...` and `frame_0007_...`, NOT `frame_0001_...` and `frame_0002_...`.

- `test_extraction_mode_defaults_to_interval` — **spelled-out mock stack:**

        def test_extraction_mode_defaults_to_interval(tmp_path, monkeypatch):
            from unittest.mock import patch, MagicMock
            from src.extractor import extract_frames

            # (a) fake binaries
            monkeypatch.setattr('shutil.which',
                lambda name: f'/fake/bin/{name}' if name in ('ffmpeg', 'ffprobe') else None)

            # (b) ffprobe stdout — valid duration JSON
            probe_result = MagicMock(returncode=0,
                stdout='{"streams":[{"duration":"10.0"}]}', stderr='')
            # (c) ffmpeg exit — success, and write one tmp frame so the rename loop has something
            def _ffmpeg_side_effect(cmd, **kw):
                # Detect the ffmpeg call vs ffprobe call by argv[0]
                if 'ffprobe' in cmd[0]:
                    return probe_result
                # ffmpeg: create a dummy tmp frame
                tmp_dir = tmp_path / 'out' / '.tmp_extract'
                tmp_dir.mkdir(parents=True, exist_ok=True)
                (tmp_dir / 'frame_0001.png').write_bytes(b'\x89PNG')
                return MagicMock(returncode=0, stdout='', stderr='')

            with patch('subprocess.run', side_effect=_ffmpeg_side_effect) as mocked:
                # Need a fake video file to satisfy Path.exists()
                fake_video = tmp_path / 'in.mp4'
                fake_video.write_bytes(b'')
                result = extract_frames(str(fake_video), str(tmp_path / 'out'), 2)

            # Assert ffmpeg was called with the interval filter
            ffmpeg_calls = [c for c in mocked.call_args_list if 'ffmpeg' in c.args[0][0]]
            assert ffmpeg_calls, 'ffmpeg was never invoked'
            cmd_str = ' '.join(ffmpeg_calls[0].args[0])
            assert 'fps=1/' in cmd_str
            assert len(result) == 1

- `test_invalid_extraction_mode_raises` — call `extract_frames(<any video>, ..., cfg={"extraction_mode": "bogus"})`; assert `FrameExtractionError` with the expected message.
  </action>
  <verify>`pytest tests/test_frame_extractor.py -v -k "not baseline"` — 4+ tests pass (excluding the slow baseline test).</verify>
  <acceptance_criteria>
    - "tests/test_frame_extractor.py exists"
    - "pytest tests/test_frame_extractor.py -k 'not baseline' reports >= 4 passed"
  </acceptance_criteria>
  <done>Tests pass with a well-specified mock stack.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pytest tests/test_frame_extractor.py -v` — all pass (including the byte-identical baseline test if the baseline video is present)
- [ ] `python main.py ./config/config.json` produces the same frame count and filenames as before this plan
- [ ] `git diff src/pipeline/pipeline_runner.py` shows exactly one changed hunk (the `cfg=config` addition)
- [ ] `grep -n '_EXTRACTORS\|_finalize_frames\|_extract_by_interval\|_validate_extraction_config' src/extractor/frame_extractor.py` shows all four helpers
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No behavior change for existing configs (backward compat proven by test + manual run)
- Code path for scene/hybrid is stubbed with a clear "not yet implemented" error pointing at plan 01-02

</success_criteria>

<output>
After completion, create `.planning/phases/01-scene-extraction/01-01-SUMMARY.md`
</output>

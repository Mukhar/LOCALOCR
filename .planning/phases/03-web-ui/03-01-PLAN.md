---
phase: 03-web-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - requirements.txt
  - src/web/__init__.py
  - src/web/__main__.py
  - src/web/services/__init__.py
  - src/web/services/db.py
  - src/web/services/event_bus.py
  - tests/test_web/__init__.py
  - tests/test_web/test_db.py
  - tests/test_web/test_event_bus.py
autonomous: true
requirements: [WEBUI-01, WEBUI-06]
user_setup: []
must_haves:
  truths:
    - "python -m localocr.web boots a minimal FastAPI server responding on http://localhost:8765"
    - "SQLite database at output/localocr.sqlite has runs and picks tables per the schema"
    - "EventBus supports async subscribe/publish with proper cleanup on unsubscribe"
    - "New dependencies installed via uv with Walmart index-url"
  artifacts:
    - "src/web/services/db.py (SQLite schema + repository)"
    - "src/web/services/event_bus.py (in-memory async pub/sub)"
    - "src/web/__main__.py (uvicorn entry point)"
    - "pyproject.toml / requirements.txt updated with fastapi, uvicorn, sse-starlette, python-multipart, aiosqlite"
  key_links:
    - "src/web/__init__.py exposes create_app() factory"
    - "db.py uses PRAGMA user_version for migrations (no Alembic)"
---

<objective>
Scaffold the FastAPI web package: minimal server that boots, SQLite
repository with schema, and the async event bus that later plans will
plug into. No routes yet beyond a health-check — that's plan 03-03.

Purpose: Get the plumbing right first. Later plans layer runtime
concerns (subprocess, SSE, templates) on this foundation.
Output: Bootable server + working DB + working event bus, all tested.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@requirements.txt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install web dependencies</name>
  <files>requirements.txt, pyproject.toml</files>
  <action>
Add to `requirements.txt` (or `pyproject.toml` if project migrates to it now):

    fastapi>=0.110
    uvicorn[standard]>=0.29
    sse-starlette>=2.0
    python-multipart>=0.0.9
    aiosqlite>=0.20
    jinja2>=3.1

Install per Walmart rules:

    uv pip install \
      --index-url https://pypi.ci.artifacts.walmart.com/artifactory/api/pypi/external-pypi/simple \
      --allow-insecure-host pypi.ci.artifacts.walmart.com \
      fastapi 'uvicorn[standard]' sse-starlette python-multipart aiosqlite jinja2

If the project isn't using a uv venv yet, create one at `.venv/` first with `uv venv`.
  </action>
  <verify>`python -c "import fastapi, uvicorn, sse_starlette, aiosqlite, jinja2"` — no ImportError.</verify>
  <acceptance_criteria>
    - "requirements.txt contains 'fastapi'"
    - "requirements.txt contains 'sse-starlette'"
    - "python -c 'import fastapi' exits 0"
  </acceptance_criteria>
  <done>Dependencies installed and pinned.</done>
</task>

<task type="auto">
  <name>Task 2: SQLite repository</name>
  <files>src/web/__init__.py, src/web/services/__init__.py, src/web/services/db.py</files>
  <action>
Create the package skeleton:
- `src/web/__init__.py` — empty or holds create_app placeholder
- `src/web/services/__init__.py` — empty

Create `src/web/services/db.py`:

    """
    SQLite repository for the LOCALOCR web UI.

    Schema versioning via PRAGMA user_version (Alembic is overkill here).
    """
    import json
    import sqlite3
    from contextlib import contextmanager
    from datetime import datetime, timezone
    from pathlib import Path
    from typing import Iterator, Optional

    SCHEMA_VERSION = 1

    _SCHEMA_SQL = """
    CREATE TABLE IF NOT EXISTS runs (
      id            INTEGER PRIMARY KEY AUTOINCREMENT,
      video_path    TEXT NOT NULL,
      config_json   TEXT NOT NULL,
      mode          TEXT NOT NULL,
      status        TEXT NOT NULL,
      started_at    TEXT NOT NULL,
      finished_at   TEXT,
      total_frames  INTEGER,
      matched_count INTEGER,
      summary_json  TEXT
    );
    CREATE TABLE IF NOT EXISTS picks (
      id             INTEGER PRIMARY KEY AUTOINCREMENT,
      run_id         INTEGER NOT NULL REFERENCES runs(id) ON DELETE CASCADE,
      analyst        TEXT,
      stock_pick     TEXT NOT NULL,
      current_price  TEXT,
      target         TEXT,
      stop_loss      TEXT,
      frame_path     TEXT,
      frame_timestamp_seconds REAL,
      transcript_context TEXT,
      raw_json       TEXT NOT NULL
    );
    CREATE INDEX IF NOT EXISTS idx_picks_run ON picks(run_id);
    CREATE INDEX IF NOT EXISTS idx_runs_started ON runs(started_at DESC);
    """

    def _now_iso() -> str:
        return datetime.now(timezone.utc).isoformat(timespec="seconds")

    def init_db(db_path: Path) -> None:
        db_path.parent.mkdir(parents=True, exist_ok=True)
        with sqlite3.connect(db_path) as conn:
            conn.execute("PRAGMA foreign_keys = ON")
            cur = conn.execute("PRAGMA user_version")
            version = cur.fetchone()[0]
            if version < SCHEMA_VERSION:
                conn.executescript(_SCHEMA_SQL)
                conn.execute(f"PRAGMA user_version = {SCHEMA_VERSION}")
                conn.commit()

    @contextmanager
    def connect(db_path: Path) -> Iterator[sqlite3.Connection]:
        conn = sqlite3.connect(db_path)
        conn.row_factory = sqlite3.Row
        conn.execute("PRAGMA foreign_keys = ON")
        try:
            yield conn
            conn.commit()
        except Exception:
            conn.rollback()
            raise
        finally:
            conn.close()

    def insert_run(db_path: Path, video_path: str, config: dict) -> int:
        with connect(db_path) as conn:
            cur = conn.execute(
                "INSERT INTO runs (video_path, config_json, mode, status, started_at) "
                "VALUES (?, ?, ?, 'running', ?)",
                (video_path, json.dumps(config), config.get("mode", "accurate"), _now_iso()),
            )
            return cur.lastrowid

    def finalize_run(db_path: Path, run_id: int, status: str, summary: Optional[dict] = None) -> None:
        with connect(db_path) as conn:
            conn.execute(
                "UPDATE runs SET status=?, finished_at=?, "
                "total_frames=?, matched_count=?, summary_json=? WHERE id=?",
                (status, _now_iso(),
                 (summary or {}).get("total_frames"),
                 (summary or {}).get("matched_frames"),
                 json.dumps(summary) if summary else None,
                 run_id),
            )

    def list_runs(db_path: Path, limit: int = 100) -> list[dict]:
        with connect(db_path) as conn:
            return [dict(r) for r in conn.execute(
                "SELECT * FROM runs ORDER BY started_at DESC LIMIT ?", (limit,)
            ).fetchall()]

    def get_run(db_path: Path, run_id: int) -> Optional[dict]:
        with connect(db_path) as conn:
            row = conn.execute("SELECT * FROM runs WHERE id=?", (run_id,)).fetchone()
            return dict(row) if row else None

    def insert_pick(db_path: Path, run_id: int, pick: dict) -> int:
        with connect(db_path) as conn:
            cur = conn.execute(
                "INSERT INTO picks (run_id, analyst, stock_pick, current_price, "
                "target, stop_loss, frame_path, frame_timestamp_seconds, "
                "transcript_context, raw_json) "
                "VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?)",
                (run_id, pick.get("analyst"), pick.get("stockPick", ""),
                 pick.get("current_price"), pick.get("target"), pick.get("stop_loss"),
                 pick.get("frame_path"), pick.get("frame_timestamp_seconds"),
                 json.dumps(pick.get("transcript_context")) if pick.get("transcript_context") else None,
                 json.dumps(pick)),
            )
            return cur.lastrowid

    def list_picks(db_path: Path, run_id: int) -> list[dict]:
        with connect(db_path) as conn:
            rows = conn.execute("SELECT * FROM picks WHERE run_id=? ORDER BY id",
                                (run_id,)).fetchall()
            return [dict(r) for r in rows]

    def delete_run(db_path: Path, run_id: int) -> None:
        with connect(db_path) as conn:
            conn.execute("DELETE FROM runs WHERE id=?", (run_id,))
  </action>
  <verify>`python -c "from src.web.services import db; from pathlib import Path; db.init_db(Path('/tmp/test_localocr.sqlite')); print('ok')"`.</verify>
  <acceptance_criteria>
    - "src/web/services/db.py contains 'CREATE TABLE IF NOT EXISTS runs'"
    - "src/web/services/db.py contains 'PRAGMA user_version'"
    - "src/web/services/db.py exposes insert_run, finalize_run, list_runs, get_run, insert_pick, list_picks, delete_run"
  </acceptance_criteria>
  <done>Repository module works.</done>
</task>

<task type="auto">
  <name>Task 3: Event bus</name>
  <files>src/web/services/event_bus.py</files>
  <action>
Create `src/web/services/event_bus.py`:

    """
    In-memory async pub/sub for streaming pipeline progress to SSE clients.

    Not durable, not multi-process — a full page reload rejoins from the
    persisted DB state and picks up new events from here.
    """
    import asyncio
    from collections import defaultdict
    from typing import AsyncIterator

    class EventBus:
        def __init__(self) -> None:
            self._queues: dict[int, list[asyncio.Queue]] = defaultdict(list)
            self._lock = asyncio.Lock()

        def publish(self, run_id: int, event: dict) -> None:
            for q in list(self._queues.get(run_id, [])):
                q.put_nowait(event)

        async def subscribe(self, run_id: int) -> AsyncIterator[dict]:
            q: asyncio.Queue = asyncio.Queue()
            async with self._lock:
                self._queues[run_id].append(q)
            try:
                while True:
                    event = await q.get()
                    yield event
                    if event.get("type") == "done":
                        return
            finally:
                async with self._lock:
                    if q in self._queues.get(run_id, []):
                        self._queues[run_id].remove(q)
                    if not self._queues[run_id]:
                        self._queues.pop(run_id, None)

    # Module-level singleton
    event_bus = EventBus()
  </action>
  <verify>Tests in Task 5 cover this.</verify>
  <acceptance_criteria>
    - "src/web/services/event_bus.py contains 'class EventBus'"
    - "src/web/services/event_bus.py exports 'event_bus' singleton"
  </acceptance_criteria>
  <done>Event bus implemented.</done>
</task>

<task type="auto">
  <name>Task 4: Minimal FastAPI app + __main__ entry</name>
  <files>src/web/__init__.py, src/web/__main__.py</files>
  <action>
`src/web/__init__.py`:

    from fastapi import FastAPI
    from pathlib import Path

    from .services.db import init_db

    def create_app(db_path: Path | None = None) -> FastAPI:
        app = FastAPI(title="LOCALOCR", version="1.1")
        app.state.db_path = db_path or Path("output/localocr.sqlite").resolve()
        init_db(app.state.db_path)

        @app.get("/health")
        def health():
            return {"status": "ok", "db_path": str(app.state.db_path)}

        return app

`src/web/__main__.py`:

    """Entry point: python -m src.web (or python -m localocr.web after packaging)."""
    import uvicorn

    from src.web import create_app

    def main() -> None:
        app = create_app()
        uvicorn.run(app, host="127.0.0.1", port=8765, log_level="info")

    if __name__ == "__main__":
        main()

Note: `python -m localocr.web` requires a proper package name via pyproject.toml; for now `python -m src.web` works. Alias to `localocr.web` when packaging is added (defer to a followup — YAGNI for this plan).
  </action>
  <verify>
    In one terminal: `python -m src.web` — server logs "Uvicorn running on http://127.0.0.1:8765".
    In another: `curl http://127.0.0.1:8765/health` — returns `{"status":"ok",...}`.
  </verify>
  <acceptance_criteria>
    - "src/web/__init__.py contains 'def create_app('"
    - "src/web/__main__.py contains 'uvicorn.run'"
    - "GET /health returns 200 with status ok"
  </acceptance_criteria>
  <done>Server boots and responds.</done>
</task>

<task type="auto">
  <name>Task 5: Unit tests for db + event_bus</name>
  <files>tests/test_web/__init__.py, tests/test_web/test_db.py, tests/test_web/test_event_bus.py</files>
  <action>
**tests/test_web/test_db.py** — use `tmp_path` fixture for isolated SQLite files:

- `test_init_db_creates_tables`
- `test_init_db_is_idempotent` (call twice, no error)
- `test_insert_and_get_run`
- `test_list_runs_ordered_desc`
- `test_finalize_run_updates_status_and_summary`
- `test_insert_and_list_picks`
- `test_delete_run_cascades_to_picks`

**tests/test_web/test_event_bus.py** — pytest-asyncio (or asyncio.run manually):

- `test_publish_delivers_to_subscriber`
- `test_multiple_subscribers_all_receive`
- `test_subscriber_cleanup_on_unsubscribe` — subscribe, break out of iteration, verify queue removed from _queues
- `test_done_event_terminates_subscription`

Install pytest-asyncio if not already: `uv pip install pytest-asyncio` via Walmart index.
  </action>
  <verify>`pytest tests/test_web/ -v` — all pass.</verify>
  <acceptance_criteria>
    - "pytest tests/test_web/ reports >= 11 passed"
  </acceptance_criteria>
  <done>Tests green.</done>
</task>

</tasks>

<verification>
- [ ] `pytest tests/ -v` — all tests pass (Phase 1 + 2 + this plan's tests)
- [ ] `python -m src.web &` boots server, `curl http://localhost:8765/health` returns 200
- [ ] `output/localocr.sqlite` exists after server boot with 2 tables
- [ ] Kill the server: `kill %1` (or find PID)
</verification>

<success_criteria>

- Server boots and health-checks
- DB is initialized with correct schema
- Event bus is testable and cleanly cleans up after subscribers
- No routes beyond /health yet — that's plan 03-03

</success_criteria>

<output>
After completion, create `.planning/phases/03-web-ui/03-01-SUMMARY.md`
</output>
